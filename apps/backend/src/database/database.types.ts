// Esquema de tablas para Kysely. Todavia vacio a proposito — este prompt
// es solo scaffolding, sin logica de negocio real (aulas, profesores, etc).
// Se va a ir poblando cuando definamos el esquema real de la base.
export interface Database {}
